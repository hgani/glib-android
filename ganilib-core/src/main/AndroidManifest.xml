<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.gani.lib">

  <!-- TODO: Consider moving QR code scanner to a submodule -->
  <uses-permission android:name="android.permission.CAMERA" />
  <uses-feature android:name="android.hardware.camera" />

  <application
      android:allowBackup="true"
      android:label="@string/app_name"
      android:supportsRtl="true"
      android:theme="@style/AppTheme">

    <activity android:name=".ui.json.actions.screens.JsonUiScreen" />

    <!--
    <activity
        android:name=".notification.Alert"
        android:theme="@style/FakeDialogNoTitleBar" />

        -->
    <activity
        android:name=".dialog.GRestDialogProgress"
        android:theme="@style/FakeDialogNoTitleBar" />
  </application>

</manifest>
